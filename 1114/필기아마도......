ch10
pipe : this is pipe to move some oil or liquid ㅇㅇ

 - pipedemo.c
apipe[2]는 파일 디스크립터 두개 받을 곳임 ㅇㅇ 그림보면 이해하기 쉬움 ㅇㅇ

파이프 만든 상태에서 포크하면 파이프는 복제하지 않는 듯. 있는 파이프 그대로 쓰는 듯.

- pipedemo2.c
child는 while 문에서 멈춰있네 ㅇㅇ 파이프에 여러 값 동시에 보내도 덮어써지진 않는듯
queue 처럼 파이프 안에 쌓인대 ㅇㅇ 그래서 먼저 입력된 값 출력해주고 차차 그렇게 ㅇㅇ

10.6.3
쓸거는 stdio로 복제시키고 안 쓸거는 끄고 ㅇㅇ 

pipe.c
else 없는 이유 : eleclp실행시키면 기존 프로세스가 새로운 프로세스로 업데이트 됨 그래서 else가 의미가 없다.




ch11
파일스트림과 디스크립터의 차이 : 파일디스크립터는 lower lever용, stream은 higher level 그래서 다른 함수를 사용하기 위해서는 파일 스트림을 사용해야함.

근데 파이프는 관련된 프로세스끼리만 쓸 수 있음 패런트차일드같이, 그리고 같은 컴퓨터 안에서만 가능함
